#!/usr/bin/env bash
set +x

Docker_USERNAME=$1
DOCKER_PASSWORD=$2


docker login ${DOCKER_IMAGE_REPOSITORY} --username ${DOCKER_USERNAME} --password ${DOCKER_PASSWORD}
docker image push ${DOCKER_IMAGE_NAME}