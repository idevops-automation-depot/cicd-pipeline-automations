#!/usr/bin/env bash
set +x

DOCKER_USERNAME=$1
DOCKER_PASSWORD=$2


docker login --username ${DOCKER_USERNAME} --password ${DOCKER_PASSWORD} idevopsio.azurecr.io
docker image push ${DOCKER_IMAGE_NAME}